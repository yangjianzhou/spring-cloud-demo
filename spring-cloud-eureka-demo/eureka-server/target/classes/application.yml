spring:
  application:
    name: eureka-server

server:
  port: 8080
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
#    service-url: http://localhost:5002/eureka/
  instance:
    hostname: 127.0.0.1
